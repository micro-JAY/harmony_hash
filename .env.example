# Client-side Vite env. Copy to `.env` (gitignored) only when adding local Vite
# flags. Hanz has no browser provider configuration: the Worker owns its model,
# instructions, voice, tools, and standard API key. Never put secrets in VITE_
# variables because Vite inlines them into the browser bundle.
