# Acceptance Ledger

This ledger maps the user's current correction request to authoritative evidence. A row is complete only when both implementation and rendered/test evidence are present.

| ID | Required end state | Baseline evidence | Completion evidence |
|---|---|---|---|
| A1 | HASHER / TUNE TOOLBOX / FRET FINDER navigation is geometrically centered. | Browser geometry at 1440×900 measured the navigation center 91px left of viewport center; `Header.tsx` uses a flex row whose center depends on unequal brand/utility widths. | `Header.tsx` now uses a three-region grid. Playwright measures the English and Japanese navigation center within 1px of the content center; desktop screenshots are inspected and tablet/mobile layouts remain contained. |
| A2 | Four centered preset buttons open Major, Minor, Modal, and Advanced dialogs. | Browser DOM/screenshot and `ProgressionInput.tsx` show category buttons followed by an active subgroup tab strip and one horizontally clipped progression list, not dialogs. | Browser and Playwright verify four centered buttons in the bordered surface; each opens the localized `PresetCategoryDialog`, supports pointer/keyboard/Escape/outside dismissal, and restores focus. |
| A3 | Every documented preset is visible: Major 23, Minor 21, Modal 13, Advanced 5. | `PROGRESSION_LIBRARY` and `docs/hh-library.md` contain 62 entries, but Browser exposed only the active 10 Major Foundation buttons; screenshots 1–4 show the intended complete category inventories. | Source/document/dialog parity tests assert exact counts `23/21/13/5`, unique entries, and all 744 preset/key combinations resolve through the production dictionary. English Major and Japanese Minor dialogs were inspected directly. |
| A4 | Focused/selected chips expose `X`; a threshold-qualified pointer release or actual native drop anywhere outside the composer removes; persistent handles/arrows/`+`/remove strip remain absent. | The first correction added `X` and an explicit target, but arbitrary outside release remains inert and contradicts the post-PR instruction. | Selected/focused chips expose one compact `X`; actual native mouse drops and threshold-qualified touch/pen releases anywhere outside remove through the shared transaction. Escape, `pointercancel`, lost capture, detached-source dragend, no-drop dragend, and external grid-source paths remain inert. Pointer, keyboard, native drag, and mobile Playwright paths pass. |
| A5 | Major green, Minor orange, Dominant deep red, Suspended yellow, Diminished pink, Augmented white on every visible dictionary-chord label. | The first correction covers grid headers, output cards, modifier, and comparison; composer chips, Circle chord labels, and FRET FINDER overlay labels remain generic. | The shared classifier/presentation now covers composer chips, grid headers, rendered cards, modifier/comparison titles and choices, Circle and Minor Blend chord labels, FRET FINDER overlays, and NOTE NEURAL NETWORK chord nodes. Six-family real-surface tests preserve selection, relationship, score, focus, playback, lock, and Hanz cues. |
| A6 | Root rows stay blue and percentage scores keep the match gradient. | Current grid owns separate root and match layers; correction must not collapse them into family colors. | Semantic UI tests and Browser inspection prove blue root labels remain independent from family headers; modifier and suggestion percentages retain the shared low/mid/high match gradient and readable numeric text. |
| A7 | Guitar `Fingering / Intervals / Notes` is centered independently of `Modify`; Piano has no label selector. | Screenshot 7 and `ChordCard.tsx` use `justify-between`, centering the toggle only in leftover space. | The Guitar toolbar uses a symmetric three-region grid; Browser geometry measures the mode control center within 0.01px of the card center. Desktop/mobile tests cover wrapped containment, and Piano cards render no label selector. |
| A8 | Modifier is a modal with a `Top picks` section; chord names use family colors and percentages use match colors. | PR #75 already uses `AccessibleDialog`, but color and percentage semantics require verification. | The accessible `Modify … chord` dialog contains a ranked `Top picks` section, traps/restores focus, closes with Escape or selection, gives every option a family treatment, and combines match-gradient percentage text with an explicit screen-reader score. Pointer, keyboard, and mobile flows pass. |
| A9 | Piano style selectors show only applicable choices and do not create uneven empty tracks, fixed rows, or left-biased partial rows. | The first correction filters styles but still uses an unconditional four-column grid and fixed 4.75rem height, leaving empty tracks for three/five/six-choice inventories. | Applicable-only buttons use centered, content-sized wrapping below the keyboard with no reserved empty tracks. Browser geometry proves 3/6/3/7 choices use centered one/two-row layouts at 12px while all four desktop cards remain exactly 446px high and all four tablet cards exactly 429px high. |
| A10 | Every active Piano key is visible inside a full-width, fixed-size card keyboard. | Browser measured each primary Piano keyboard at 630px inside a 457px desktop card and marked all three clipped; the same fixed 630px strip remains clipped at 820px and 375px. | Shared proportional geometry renders all 21 white and 15 black keys inside each full-width keyboard without horizontal scrolling. Every active key is within keyboard/card bounds for wide and extended voicings at desktop, tablet, and 375px; card size remains stable. |
| A11 | Piano voicing comparison remains a contained accessible popup with only applicable options. | PR #75 already moved comparison into `AccessibleDialog`; regression proof is incomplete. | Pointer/keyboard tests verify an accessible modal with applicable-only choices, bounded internal layout, selection, Escape/outside close, and trigger-focus restoration on desktop and mobile. |
| A12 | English/Japanese, reduced motion, lock/variant/style state, playback, Hanz focus, and stale-agent safety remain intact. | These are existing cross-cutting contracts. | Build/lint and 1,270 unit/component tests pass; complete Playwright passes 171/171, including real touch removal/cancellation and all existing Hanz, playback, share, agent-race, Toolbox, FRET FINDER, Japanese, reduced-motion, and responsive regressions. Browser QA is console-clean, Worker dry-run passes, npm reports zero production vulnerabilities, and independent post-fix reviews report no remaining P0–P2 finding. |
| A13 | Media actions read `RANDOMIZE (UNLOCKED VOICES)`, `PLAY`/`STOP`, `SHARE`, and `IMPROV INSIGHT`; Play and Share retain icons; no Hanz media button exists. | Runtime labels are present and Hanz is prompt-help-only, but visible PLAY and the two icon contracts are not directly asserted. | Guitar/Piano browser coverage asserts the exact visible text, Play and Share icons, starting/playing accessibility state, and the absence of a Hanz action. Hanz remains available only from progression-prompt help. |
| A14 | Every IMPROV launch and internal selection uses the dedicated pale-pink system; score/degree colors remain independent and Style is neutral. | HASHER launch is near-white pink, but Toolbox/Circle launchers remain academy blue; the per-chord selector uses HASHER gold and generic soft tokens fragment the contract. | HASHER, Toolbox, Circle, and in-panel controls share the dedicated `music-insight` action/surface token family. Computed-style tests keep Style neutral and preserve independent match-gradient and named-degree colors. |
| A15 | IMPROV vocabulary `?` explains Motion, Tension, Palette, Style and every option; close restores focus. | The dialog is implemented, but browser coverage checks option names without all headings/descriptions. | The localized accessible vocabulary dialog covers all four groups, their option inventories, category explanations, and option explanations; Escape/backdrop/close behavior and exact launcher-focus restoration pass in English and Japanese. |
| A16 | IMPROV scale titles and notes use the same Named Degrees palette as SCALE SYNTHESIA and FRET FINDER; scale typography is correct. | Notes already share `intervalColor()` and the display font is correct; scale titles remain generic white and the requested Ab/minor-third cross-surface example is unproved. | Scale roots and individual notes consume the shared named-degree palette and display typography. Rendered Playwright proves the F Major Blues `Ab` minor-third color is identical across IMPROV, SCALE SYNTHESIA, and FRET FINDER; Root and other interval roles retain the same source. |
| A17 | IMPROV remains inside the centered content width with compact notes and an aligned short match meter. | Current result geometry is capped and tests bound panel/meter width, but the complete user contract is not recorded in OpenSpec. | The panel is capped at the centered 72rem rail, each meter at 14rem, and note spacing remains compact. Desktop Browser measured the panel at 1,152px; tablet/mobile tests and direct 375px inspection prove containment with no document overflow. |
| A18 | Circle/Toolbox opens IMPROV locally without navigating to HASHER and restores the origin on close. | Runtime already renders a theory-origin panel without workspace mutation; current launcher color is wrong and direct origin-local assertions are incomplete. | Circle/Toolbox opens IMPROV in place, preserves workspace/context/timeline/instrument state, and restores the exact origin trigger on close. Dedicated focus-region and launcher-color coverage pass without a HASHER navigation round trip. |

## Baseline Reference Images

- Major catalogue: user image 1 (`CleanShot 2026-07-16 at 16.02.55.png`)
- Minor catalogue: user image 2 (`CleanShot 2026-07-16 at 16.11.25.png`)
- Modal catalogue: user image 3 (`CleanShot 2026-07-16 at 16.11.30.png`)
- Advanced catalogue: user image 4 (`CleanShot 2026-07-16 at 16.11.39.png`)
- HASHER layout/navigation: user image 5 (`CleanShot 2026-07-16 at 16.01.38.png`)
- Chord-grid family headers: user image 6 (`CleanShot 2026-07-16 at 16.17.01.png`)
- Guitar cards/control alignment: user image 7 (`CleanShot 2026-07-16 at 16.17.49.png`)
- Piano cards/voicing geometry: user image 8 (`CleanShot 2026-07-16 at 16.19.12.png`)

## Measured PR #75 Baseline

- Desktop 1440×900: navigation center delta `-91px`; visible Major progression buttons `10`; composer removal buttons `0`; Guitar cards `457×361px`; Piano cards `457×387px`; Piano keyboards `630px` and clipped.
- Tablet 820×1000: navigation wraps near center; visible Major progression buttons `10`; Piano keyboards remain `630px` and clipped; no document-level overflow.
- Mobile 375×812: navigation wraps near center; visible Major progression buttons `10`; Piano keyboards remain `630px` and clipped; no document-level overflow because the card hides the excess.
- Page identity: `HARMONY HASH — TONARI LABS`; no framework overlay. The expected Vite-only service-unavailable state is present because `npm run dev` does not serve Worker API routes.
